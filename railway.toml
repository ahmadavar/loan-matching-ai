[build]
dockerfilePath = "backend/Dockerfile"
buildContext = "."

[deploy]
startCommand = "scripts/entrypoint.sh"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
